#!/bin/bash

# Определяем, где лежит этот скрипт
HERE="$(dirname "$(readlink -f "${0}")")"

# Указываем нашему Wine, где его "дом"
export WINEPREFIX="$HERE/prefix"
export WINEDEBUG=-all

# Указываем, использовать наш скачанный Wine, а не системный
export PATH="$HERE/wine/bin:$PATH"

# Если префикса (виртуальной винды) еще нет, wine его создаст сам при первом запуске.
# Но так как мы уже установили прогу выше, файлы лежат в дефолтном ~/.wine
# Давай сделаем финт ушами.

# ПРАВИЛЬНЫЙ ЗАПУСК:
# Мы запустим установленный EXE, используя наш локальный Wine.
# Путь внутри префикса обычно такой:
"$HERE/wine/bin/wine" "/home/user/.wine/drive_c/Program Files (x86)/WOMic/WOMicClient.exe" "$@"
